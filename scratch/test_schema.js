import { CanonicalSchema } from ../src/schemas/canonical.js; import { transformToCanonical } from ../src/utils/dataFixer.js; const tournament = { id: t1, playersPerTeam: 5, entryFee: 0 }; const formData = { teamName: A, teamTag: B, teamRegion: C, logoLink: D, players: [{ign:ign, discord:D, steam:S, faceit:F, faceitLevel: 10, faceitElo: 2000}] }; const can = transformToCanonical(tournament, formData); const res = CanonicalSchema.safeParse(can); if (!res.success) console.log(JSON.stringify(res.error.format(), null, 2)); else console.log(SUCCESS);
